Settings:


todo
    TIME_ZONE = 'Europe/Berlin'
    in CMD generate new secret key:
        python -c 'from django.core.management.utils import get_random_secret_key; \
      print(get_random_secret_key())'
      and paste it...
      ALLOWED_HOSTS = ['127.0.0.1', '.pythonanywhere.com']
